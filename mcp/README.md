# MCP integrations

This directory will hold reviewed MCP manifests and JL Agent policy metadata.
Server source and credentials are not vendored here. Every server must declare
transport, command or URL, requested permissions, health check, data boundary,
and source revision before it can be enabled.

No MCP server is enabled by default in Phase 1.
